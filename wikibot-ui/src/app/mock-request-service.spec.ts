import { MockRequestService } from './mock-request-service';

describe('MockRequestService', () => {
  it('should create an instance', () => {
    expect(new MockRequestService()).toBeTruthy();
  });
});
